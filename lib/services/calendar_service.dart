import '../models/task.dart';

class CalendarService {
  static Future<void> syncTaskWithCalendar(Task task) async {
    // TODO: сделать синхронизацию
    await Future.delayed(Duration(milliseconds: 300));
  }
}
